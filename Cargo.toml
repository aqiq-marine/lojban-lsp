[package]
name = "lojban_parser"
version = "0.1.0"
edition = "2024"

[dependencies]
rowan = "0.17.0"
tower-lsp = "0.20.0"
tokio = { version = "1", features = ["io-std", "sync", "macros", "rt-multi-thread"] }
phf = { version = "0.14.0", features = ["macros"] }
